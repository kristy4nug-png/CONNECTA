name: Release and Deploy

on:
  push:
    branches: [ master, main ]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Install dev dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build release ZIP
        run: |
          mkdir -p releases
          VERSION=$(node -p "require('./package.json').version")
          DATE=$(date -u +%Y%m%d)
          zip -r "releases/atlas-web-${VERSION}-${DATE}.zip" index.html privacy.html manifest.webmanifest service-worker.js app-version.json assets icons *.js

      - name: Prepare gh-pages folder
        run: |
          rm -rf releases/gh-pages || true
          mkdir -p releases/gh-pages
          cp -r index.html privacy.html *.js manifest.webmanifest service-worker.js app-version.json assets icons releases/gh-pages

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./releases/gh-pages
          publish_branch: gh-pages
